services:
  mongodb:
    image: mongo:6
    container_name: silid-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:alpine
    container_name: silid-redis
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
